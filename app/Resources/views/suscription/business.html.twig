{% extends 'bootstrap.html.twig' %}

{% block body %}
    <div class="container">
        <h1>{{'suscription.business'|trans}}</h1>
    </div>
    <div class="container">
       
        {{ form_start(form) }}
        {{ form_errors(form) }}
        <div class="form-group">
            {{ form_widget(form) }}
        </div>
       

        {{ form_end(form) }}

    </div>  

{% endblock %}


