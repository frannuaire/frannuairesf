<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('homepage') }}">{{'site.title'|trans}}</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('homepage') }}">{{'home'|trans}}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('board') }}">{{'dashboard'|trans}}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('homeadmin') }}">{{'homeadmin'|trans}}</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('logout') }}">{{'Logout'|trans}}</a>
            </li>
        </ul>
        <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="text" placeholder="{{'search'|trans}}" aria-label="{{'search'|trans}}">
            <button class="btn btn-outline-warning my-2 my-sm-0" type="submit">{{'search'|trans}}</button>
        </form>
    </div>
</nav>
